# SmartData.Contracts

Shared contracts and provider interfaces for the SmartData framework.

## Features

- Provider interfaces for database, schema, and raw data operations
- Shared contracts referenced by both client and server packages
