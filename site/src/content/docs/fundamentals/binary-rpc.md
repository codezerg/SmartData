---
title: Binary RPC
description: Wire protocol, endpoint layout, and the single /rpc route.
---

> TODO — `application/x-binaryrpc`, `CommandRequest`/`CommandResponse`, double-serialized `Args`, `GetDataAsJson()` for debugging, `/health` endpoint.
