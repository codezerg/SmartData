---
title: Database context
description: IDatabaseContext — queries, transactions, cross-procedure calls.
---

> TODO — `ctx.GetTable<T>()` composition, `ctx.BeginTransaction()`, `ctx.ExecuteAsync<T>` + `ctx.QueueExecuteAsync`, per-procedure scoping.
