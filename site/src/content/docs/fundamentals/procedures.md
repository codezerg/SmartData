---
title: Procedures
description: The stored-procedure pattern — classes, parameters, lifecycle, errors.
---

> TODO — cover `StoredProcedure<T>` / `AsyncStoredProcedure<T>`, public-property parameter binding, naming (`Class` → `usp_class`), `IProcedureService` vs `IAuthenticatedProcedureService`, `RaiseError` semantics.

Source: `docs/SmartData.Guide.md`, `CLAUDE.md` "Core concepts".
